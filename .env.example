# URL of your TubeInspect Downloader backend (Render, Koyeb, local, etc.)
# Leave empty to use same-origin /api endpoints (only works if backend is hosted with frontend)
VITE_API_BASE=https://tubeinspect-api.onrender.com
